VITE_API_BASE=/api
# Backend URL for the Vite proxy (server-side, not baked into the bundle).
# Set to http://localhost:8000 when running `npm run dev` outside Docker.
# In Docker, docker-compose.yml overrides this to http://backend:8000.
VITE_BACKEND_URL=http://localhost:8000
